---
// @ts-nocheck
import SideBar from "../components/SideBar.astro";
import MainLayout from "../layout.astro/MainLayout.astro";

const ticketmasterApi = import.meta.env.TM_KEY;

    const base = "https://app.ticketmaster.com/discovery/v2/events.json?apikey=" + ticketmasterApi;
    const response = await fetch(base);
    const data = await response.json();

    console.log(data);

	console.log("Hello world");
---

<MainLayout title="API Events">
	<SideBar />

	<!-- <h1>Astro</h1> -->

		<div id="map"></div>

		<!-- <p>ik heb {todos.length}</p>
		<ul>
			{todos.map((todo) => <li>{todo.activity}</li>)}
		</ul> -->

</MainLayout>

